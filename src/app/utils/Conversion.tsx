export const kelvinToCelsius = (kelvin: number) => {
    return kelvin - 273.15;
};

export const scaleRadius = (radius: number) => {
    return radius / 10000;
}

export const scaleOrbit = (orbit: number) => {
    return orbit / 100000;
}